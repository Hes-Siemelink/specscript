Define command:
  Get full name:
    input schema:
      type: object
      properties:
        first_name: string
        last_name: string
    output schema:
      type: string
    output: ${input.first_name} ${input.last_name}

Get full name:
  first_name: Alice
  last_name: Bob

---
Expected output: Alice Bob
