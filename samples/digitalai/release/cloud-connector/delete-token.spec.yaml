Script info:
  description: Prepares a Remote Runner user in Release

Input schema:
  type: object
  properties:
    token_id:
      description: Token UID

---
Connect to: Digital.ai Release

DELETE:
  path: /tokens/users/${input.token_id}

Print: Token ${input.token_id} deleted