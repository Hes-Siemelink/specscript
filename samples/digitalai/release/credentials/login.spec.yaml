Script info: Logs in to Digital.ai Release

Get credentials: Digital.ai Release
As: ${endpoint}

---
If:
  empty: ${endpoint}
  then:
    Create: { }
    As: ${endpoint}

---
Http session:
  name: digital-ai-release
  url: ${endpoint.url}
  username: ${endpoint.username}
  password: ${endpoint.password}

Output: Connected to ${endpoint.name}