Input schema:
  type: object
  properties:
    folderId:
      description: Full folder ID
    title:
      description: Folder title
      default: New Folder

Connect to: Digital.ai Release

POST:
  path: /fixtures/folders
  body:
    - type: xlrelease.Folder
      id: ${folderId}
      title: ${title}
