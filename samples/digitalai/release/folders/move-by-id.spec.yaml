Script info: Moves a folder

Input schema:
  type: object
  properties:
    source:
      type: string
      description: Source folder ID
    target:
      type: string
      description: Target folder ID

---
Connect to: Digital.ai Release

POST:
  path: /api/v1/folders/${source}/move?newParentId=${target}
  body: { }

