Input parameters:
  id:
    description: Full ticket identifier (e.g. TICKET-007)

Connect to: Ticket DB

Print: Getting ticket ${id}

SQLite:
  file: db/tickets.db
  query: |
    SELECT
      *
    FROM ticket
    WHERE id = '${id}'  
