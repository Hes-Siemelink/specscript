Input schema:
  type: object
  properties:
    state:
      type: string
      description: Two-letter US state code (e.g. CA, NY)

Mcp call tool:
  name: get_alerts
  server:
    url: http://localhost:25096/mcp
  arguments:
    state: ${input.state}
