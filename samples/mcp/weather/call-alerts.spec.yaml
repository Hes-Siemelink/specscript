Input schema:
  type: object
  properties:
    state:
      type: string
      description: Two-letter US state code (e.g. CA, NY)

Mcp call tool:
  tool: get_alerts
  server:
    url: http://localhost:25096/mcp
  input:
    state: ${input.state}
