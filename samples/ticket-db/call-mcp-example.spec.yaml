---
Call Mcp tool:
  transport:
    type: sse
    url: http://localhost:8080
  tool: get_ticket
  arguments:
    id: TICKET-001

Print:
  The ticket we asked for: ${output}

---
Call Mcp tool:
  transport:
    type: sse
    url: http://localhost:8080
  tool: list_tickets

Print:
  All tickets: ${output}
