Tests:

  Only once do:

    Output: one

    Repeat:
      Output: done
      until: done

  Counting:

    Output: 1

    Repeat:
      Append: 1
      until: 10

  Append output:

    Output: one

    Repeat:
      Output: ${output} one
      until:
        item: ${output}
        equals: one one one

    Expected output: one one one

  Repeat checks Do result:

    Output: one

    Repeat:
      Do:
        - Output: one
        - Output: two
      until: two
