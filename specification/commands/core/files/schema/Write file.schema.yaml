$schema: https://json-schema.org/draft/2020-12/schema

oneOf:
  - type: string

  - type: object
    properties:
      file:
        type: string
      content:
        type:
          - any
    additionalProperties: false

message:
  oneOf: "Write file requires a content value, or an object with 'file' and 'content'."
