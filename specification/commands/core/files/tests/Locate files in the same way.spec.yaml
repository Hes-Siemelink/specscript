Tests:

  Read file from working directory (short way):

    Read file: specification/commands/core/files/tests/content/plain.yaml

    Expected output:
      loaded: true

  Read file from working directory (file property):

    Read file:
      file: specification/commands/core/files/tests/content/plain.yaml

    Expected output:
      loaded: true

  Read file relative to script:

    Read file: ${SCRIPT_HOME}/content/plain.yaml

    Expected output:
      loaded: true

  Read file relative to script (resource property):

    Read file:
      resource: content/plain.yaml

    Expected output:
      loaded: true

  Run script from same directory (short way):

    Run script: greet.spec.yaml

    Expected output: Hello world!

  Run script from working directory (file property):

    Run script:
      file: specification/commands/core/files/tests/greet.spec.yaml
      input:
        name: Fay

    Expected output: Hello Fay!

  Run script relative to script:

    Run script:
      file: ${SCRIPT_HOME}/greet.spec.yaml
      input:
        name: Ray

    Expected output: Hello Ray!

  Run script relative to script (resource property):

    Run script:
      resource: greet.spec.yaml
      input:
        name: Ray

    Expected output: Hello Ray!

  Shell command in same directory as script:

    Shell: sh ${SCRIPT_HOME}/hello.sh

    Expected output: Hello World

  Shell command from current directory (short way):

    Shell: specification/commands/core/files/tests/hello.sh

    Expected output: Hello World

  Shell command in same directory as script (resource property):

    Shell:
      resource: sh hello.sh

    Expected output: Hello World
