Tests:

  Save output to file:

    Output:
      test: true
      something: else

    Write file: ${SCRIPT_TEMP_DIR}/specscript-save-as-test.yaml

    Read file: ${SCRIPT_TEMP_DIR}/specscript-save-as-test.yaml

    Expected output:
      test: true
      something: else
