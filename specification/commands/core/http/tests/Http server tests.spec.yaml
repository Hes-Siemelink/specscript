Tests:

  Start and stop server:

    HTTP server:
      name: test-server
      port: 25001
      endpoints:
        /hello:
          get:
            output: Hello

    GET: http://localhost:25001/hello

    Expected output: Hello

    Stop HTTP server: test-server


  Endpoint with script output:

    HTTP server:
      name: script-server
      port: 25001
      endpoints:
        /hello:
          get:
            script:
              Output: Hello again

    GET: http://localhost:25001/hello

    Expected output: Hello again

    Stop HTTP server: script-server
