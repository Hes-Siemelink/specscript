$schema: https://json-schema.org/draft/2020-12/schema

type: object

required:
  - data

properties:
  "data": { }
  "schema":
    type:
      - object  # Inline JSON Schema
      - string  # JSON schema file
additionalProperties: false
