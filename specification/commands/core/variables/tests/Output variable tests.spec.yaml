Tests:

  Output:

    Output: a result

    Expected output: a result

  Set variable with 'As':

    Output: some more contents
    As: ${var3}

    Assert equals:
      - actual: ${var3}
        expected: some more contents

  Output variable assignment:

    ${var}: something
    ${output}: ${var}

    Expected output: something

  Output should be a list when invoking a handler with a list:

    Output:
      - one
      - two
      - three

    Expected output:
      - one
      - two
      - three

  Use numbers in interpolation:

    ${number}: 2
    Output: Number ${number}

    Expected output: Number 2
